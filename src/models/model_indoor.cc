#include "../../header/models/model_indoor.h"

using namespace std;

Model_Indoor::Model_Indoor() {

}

Model_Indoor::~Model_Indoor() {
}

bool Model_Indoor::run(Com* pCom) {

	
/*
	m_pCom = pCom;
	m_pAgentHandler = new AgentHandler();
	m_pSystemFunctions = new SystemFunctions();
	if (!this->initiate()) {
		return false;
	}
	for (int i=0; i<m_iSimulationLength;i++) {
		preStep(i);
		step();
		postStep(i);
	}
	return true; 
 */
	
	return true;
}
